// import { useCallback, useState } from 'react';

const useToggle = () => {
  // const [visible] = useState(true);
  // const open = useCallback((visible: boolean) => {
  //   visible = true;
  // }, []);
  // const close = useCallback((visible: boolean) => {
  //   visible = false;
  // }, []);
  // const toggle = useCallback((visible: boolean) => {
  //   visible = !visible;
  // }, []);
  // return { visible, open, close, toggle };
};

export { useToggle };
